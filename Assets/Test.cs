using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start ()
    {
        Debug.Log(new GetPCInfo().PCInfo());
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
